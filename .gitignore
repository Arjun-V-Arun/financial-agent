# Python
__pycache__/
*.py[cod]
.venv/

# Secrets — never commit
.env

# Derived artifacts are committed (see docs/DESIGN_DECISIONS.md) so the
# deployed app starts with a ready index instead of rebuilding on a
# resource-constrained container. Regenerate locally with:
#   python -m app.ingest && python -m app.facts && python -m app.embed

# OS / editor noise
.DS_Store
.idea/
.vscode/